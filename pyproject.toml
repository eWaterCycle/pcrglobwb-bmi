[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "ewatercycle-pcrglobwb-grpc4bmi"
version = "0.2.0"
description = "Python 3 port of PCR-GLOBWB + BMI"
authors = [
    { name = "A.B. van der Veen" }
]
dependencies = [
    "cython",
    "pydantic"
]

[tool.hatch.build]
packages = ["model"]

